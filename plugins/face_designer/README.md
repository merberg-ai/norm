# Face Designer Plugin

Beta2-pre4 plugin shell for editing N.O.R.M. procedural face packs from the web UI.

Built-in packs are read-only. Duplicate one first, then edit the copy.

Default route: `/face-designer`

API namespace: `/api/plugins/face_designer/*`
